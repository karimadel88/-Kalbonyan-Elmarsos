# iteration -> repeat items until it reaches a specified endpoint
# Loop -> Code that iterates, moving from brgining to end of the process, then starting over
# infinite Loop (never stops => program is freezes) -> Bug That can occur when the ending condition is omitted
